class reset_sequence extends uvm_sequence#(packet);
`uvm_object_utils(reset_sequence)

function new (string name="reset_sequence");
	super.new(name);
	set_automatic_phase_objection(1);//uvm-1.2 only
endfunction

task body();
      `uvm_create(req);
      req.mode=RESET;
      start_item(req);
      finish_item(req);
     	`uvm_info("RESET_SEQ","Reset Sequence : Transaction DONE",UVM_MEDIUM);
endtask
endclass
